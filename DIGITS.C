#include <stdio.h>
int main()
{
    long long A;
    int count = 0;
    printf("Enter an integer:\n ");
    scanf("%lld", &a);
    while(n != 0)
    {
        // a= a/10
        a /= 10;
        ++count;
    }
    printf("Number of digits: %d", count);
}
