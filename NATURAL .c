#include <stdio.h>
int main()
{
    int k, i, sum = 0;
    printf("Enter a positive integer:\n ");
    scanf("%d",&k);
    for(i=1; i <= n; ++i)
    {
        sum += i;   // sum = sum+i;
    }
    printf("Sum = %d",sum);
    return 0;
}
